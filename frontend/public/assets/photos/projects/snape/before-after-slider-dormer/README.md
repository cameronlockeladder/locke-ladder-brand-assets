# Snape Before/After Slider Pair

This folder groups one user-selected `before` and `after` image for future slider use.

Files:

- `before-dormer-close-up.jpg`
- `after-aerial-chimney-slate-roof-detail.jpg`

Important:

- this is a tagged pair, not a finished aligned slider set
- framing and crop still need to be matched before using these in a true before/after scrubber
- both files were resized and recompressed for web use
